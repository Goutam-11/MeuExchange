<section class="tab-panel" data-panel="meu" aria-labelledby="meu-title" hidden>
  <div class="panel-intro">
    <div>
      <span class="eyebrow">03 / MEU WORKFLOWS</span><h2 id="meu-title">
        Move from asset<br /><em>to action.</em>
      </h2>
    </div><p>
      Track the non-custodial operational layer around ATS: repo collateral,
      order matching, distributions, and the signatures that connect them.
    </p>
  </div>
  <div class="workflow-grid">
    <article class="surface workflow-card">
      <span class="workflow-number">01</span><span class="eyebrow"
        >COLLATERAL</span
      ><h3>Repo agreements</h3><strong data-count="repos">—</strong><p>
        Locked collateral and funding states held by the MEU workflow engine.
      </p><button class="quiet-button" data-refresh>Refresh state ↻</button>
    </article><article class="surface workflow-card">
      <span class="workflow-number">02</span><span class="eyebrow"
        >SECONDARY MARKET</span
      ><h3>Order book</h3><strong data-count="orders">—</strong><p>
        Open orders are matched off-chain before compliant settlement is
        prepared.
      </p><button class="quiet-button" data-refresh>Refresh state ↻</button>
    </article><article class="surface workflow-card violet-surface">
      <span class="workflow-number">03</span><span class="eyebrow"
        >DISTRIBUTIONS</span
      ><h3>Corporate actions</h3><strong data-count="distributions">—</strong><p
      >
        Distribution drafts remain visible before a wallet or custodian signs
        the payout.
      </p><button class="quiet-button" data-refresh>Refresh state ↻</button>
    </article>
  </div>
  <div class="workflow-note">
    <span>MEU API</span><code>GET /api/dashboard</code><span>→</span><span
      >persisted workflow state</span>
  </div>
</section>
